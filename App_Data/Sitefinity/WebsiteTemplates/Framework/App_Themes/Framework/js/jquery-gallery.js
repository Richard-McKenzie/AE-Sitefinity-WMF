/* ==========================================================================
 * JQUERY Specific to Gallery (transfer over to jquery functions if you are going to use them)
 * ========================================================================== */

jQuery(function($) {
// Option 1 - Single Slider
	$('.gallery-list').slick({
		  infinite: true,
	});
});